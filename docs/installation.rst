============
Installation
============

At the command line::

    $ easy_install poor-smime-sign

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv poor-smime-sign
    $ pip install poor-smime-sign
